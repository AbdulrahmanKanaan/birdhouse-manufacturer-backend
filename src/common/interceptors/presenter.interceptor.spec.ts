import { PresenterInterceptor } from './presenter.interceptor';

describe('PresenterInterceptor', () => {
  it('should be defined', () => {
    // expect(new PresenterInterceptor()).toBeDefined();
  });
});
